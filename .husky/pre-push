#!/bin/sh

echo "🚀 Iniciando pre-push..."

echo "🛠️ Verificando build..."
npm run build || {
  echo "❌ Error: Build falló - no se puede pushear"
  exit 1
}

#echo "🧪 Ejecutando pruebas..."
#npm run test -- --passWithNoTests && npm run test:e2e 2>&1 || {
#  echo "❌ Pruebas fallaron:"
#  echo "1. Ejecuta 'npm run test' para ver detalles"
#  echo "2. Verifica tests/e2e para errores"
#  exit 1
#}

echo "✅ Pre-push completado - ¡Listo para pushear!"
